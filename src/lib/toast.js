export function showToast(message) {
  // Placeholder toast implementation
  // Integrate with a real toast library later (e.g. shadcn/ui toast).
  // For now, just log.
  console.log("Toast:", message);
}
